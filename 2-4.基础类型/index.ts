let isDone:boolean = false

let age:number = 1
let binaryNumber:number = 0b11111


let firstName:string = 'a'


let u:undefined = undefined
let n:null = null

// undefined、null是所有数据类型的子类型

// Undefined代表没有赋值的基本数据类型
// （undefined 的字面意思就是：未定义的值 。这个值的语义是，希望表示一个变量最原始的状态，而非人为操作的结果）
// 基本数据类型有 string、number、boolean、undefined、null、symbol

// Null代表没有赋值的引用数据类型
// （null 的字面意思是：空值 。这个值的语义是，希望表示一个对象被人为的重置为空对象，而非一个变量最原始的状态）
// 引用数据类型有 object、array、date、function

let num:number = undefined
let string:string = null
